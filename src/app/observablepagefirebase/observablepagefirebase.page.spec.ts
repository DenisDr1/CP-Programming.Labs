import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ObservablepagefirebasePage } from './observablepagefirebase.page';

describe('ObservablepagefirebasePage', () => {
  let component: ObservablepagefirebasePage;
  let fixture: ComponentFixture<ObservablepagefirebasePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ObservablepagefirebasePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
